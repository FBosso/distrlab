# hpc_logs

PBS writes each `train_multi_node.sh` job's stdout/stderr here (`#PBS -o`/`-e`, see
`src/train_multi_node.sh`). Gitignored except for this file. Nothing to commit, just a
landing spot the job script expects to exist.
